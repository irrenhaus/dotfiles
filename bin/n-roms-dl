#!/bin/bash

NAME="$1"
SAFE_NAME=$(echo "$1" | sed 's/\(/_/g' | sed 's/\)/_/g' | sed 's/ /_/g')

mkdir "$SAFE_NAME"

(cd "$SAFE_NAME" && wget "http://www.n-roms.de/roms/gb/$NAME.zip" -O rom.zip && unzip rom.zip && rm rom.zip)
